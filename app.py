import streamlit as st
from ui import setup_page, render_header, render_sidebar
from src.data import load_clean_data

setup_page("Artraid")

render_header(
    "Artraid Analytics",
    "Добро пожаловать в систему аналитики команды CAW!"
)

render_sidebar()

df = load_clean_data()

# ── Навигационные карточки ─────────────────────────────────
st.markdown("### Разделы системы")

c1, c2 = st.columns(2)

with c1:
    with st.container(border=True):
        st.markdown("📊 **Когортный анализ**")
        st.markdown(
            "Заказы разбиты на когорты по дате продажи. "
            "Раздел показывает, как выкуп «дорастает» по дням "
            "после оформления заказа, позволяет сравнить "
            "итоговый buyout rate между когортами и оценить "
            "вклад различных источников лидов в динамику выкупа."
        )
        st.page_link("pages/01_Когортный анализ.py", label="Открыть →")

    with st.container(border=True):
        st.markdown("🤖 **Прогноз на момент оформления заказа**")
        st.markdown(
            "ML-модели первой воронки (MK1): прогноз вероятности выкупа "
            "по данным, доступным в момент оформления заказа - "
            "профиль клиента, стоимость и состав заказа (13 товарных "
            "категорий), условия оплаты и доставки, временные признаки "
            "создания лида и продажи. "
            "Визуализация важности признаков по четырём моделям."
        )
        st.page_link("pages/03_Прогноз на момент заказа.py", label="Открыть →")

with c2:
    with st.container(border=True):
        st.markdown("💰 **Финансовые потери**")
        st.markdown(
            "Оценка убытков компании от невыкупа в разрезе "
            "регионов, служб доставки, длительности доставки, "
            "способов оплаты, стоимости заказа и источников лидов. "
            "Накопительные кривые выкупа по региональным когортам."
        )
        st.page_link("pages/02_Финансовые потери и риски.py", label="Открыть →")

    with st.container(border=True):
        st.markdown("🚚 **Прогноз на момент отправки**")
        st.markdown(
            "ML-модели второй воронки (MK2): "
            "все признаки MK1, дополненные фактическими "
            "данными логистики - сроки сборки и доставки до ПВЗ. "
            "Учёт реальной длительности логистической цепочки "
            "существенно повышает точность прогноза. Визуализация важности признаков по четырём моделям."
        )
        st.page_link("pages/04_Прогноз на момент отправки.py", label="Открыть →")

st.divider()